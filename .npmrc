# npm 11+ блокирует postinstall без явного allowlist (esbuild, protobufjs и т.д.).
# Дублируем package.json allowScripts для Docker/Timeweb CI.
allow-scripts=esbuild,core-js,protobufjs,@firebase/util,@swc/core
