# Amvera: фронт mini_app (Vite → nginx). API отдельно: api.haulz.ru / Vercel.
# https://docs.amvera.ru/applications/configuration/docker.html
meta:
  environment: docker
  toolchain:
    name: docker

build:
  dockerfile: Dockerfile

run:
  containerPort: "80"
