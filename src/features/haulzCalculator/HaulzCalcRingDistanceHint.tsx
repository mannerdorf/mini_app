import React from "react";
import type { HaulzRingDistance } from "../../api/client/haulzCalculator";

type Props = {
  ringLabel: string;
  distance: HaulzRingDistance;
};

export function HaulzCalcRingDistanceHint(_props: Props) {
  return null;
}
