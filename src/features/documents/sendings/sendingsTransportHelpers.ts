/** Heuristic: plate number pattern → auto, otherwise ferry. Air via isAir when rule is ready. */
export {
  getSendingRowTransportMode,
  getSendingTransportType,
} from "../../../lib/sendingsTransportMode";
