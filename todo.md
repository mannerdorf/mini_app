# TODO — продуктовое, UI/UX и техническое ревью HAULZ

Дата: **16 сентября 2026**. Проверенная версия: **1154c0db**, локальная ветка `main`.

Исходное ревью выполнено для версии 1154c0db. Ниже сохранены описания найденных проблем; выполненные исправления отмечены `[x]` и перечислены в журнале. Отметка означает исправление в локальном коде с указанными проверками, а не развёртывание на рабочем сервере. Приоритеты отражают последствия, а не сложность реализации.

## Журнал выполнения

### 17.09.2026 — этап 12, настройки второго фактора

**Закрыт локально SEC-01. Итого 31/45 закрыто, осталось 14. SEC-02 и SEC-06 остаются открытыми.**

- **Проверки:** полный прогон — 163 файла / 794 теста успешно; backend TypeScript, production-сборка, бюджет JS и git diff --check прошли. Добавлено 27 проверок протокола, API, Redis-адаптера и React-формы. Строгий frontend typecheck остаётся красным: 296 существующих диагностик.

- Настройка, замена и отключение второго фактора требуют отдельной серверной сессии: повторный пароль, действующий OTP при включённом MFA, 5 минут и одно сохранение. Проверяется владелец; токен настройки не является общей сессией входа.
- Новый Google-секрет остаётся ожидающим до правильного кода. Старый фактор сохраняется при ошибке/отмене. Удалена передача секрета сервису qrserver.com: QR создаётся локально.
- Сервер отличает недоступный Redis от отсутствующих настроек. Состояние, потребление сессии и OTP меняются атомарно; действуют общие лимиты IP/аккаунта. Старый telegram-unlink больше не обходит проверку, бот не заменяет активный Telegram-фактор через один email-код.
- Клиент сохраняет отображаемые настройки после подтверждения сервера; удалена фоновая «тихая» запись при изменении локальных полей аккаунта. Ошибка/некорректный ответ чтения не означает выключенный MFA.
- TECH-01: удалены неподдерживаемые обработчики Switch на экране входа; осталось **296** диагностик frontend вместо 300. Новых диагностик в изменённых 2FA-модулях нет.
- Важно: обычные API всё ещё могут проверять только пароль — это отдельный SEC-02. Хранение старых паролей в localStorage также не изменено (SEC-06). Эти проблемы нельзя считать решёнными короткой сессией настройки.
- Выпуск требует согласованного обновления клиента, API и Telegram webhook; настоящие Redis/Telegram/1С и устройства не проверялись. Условия: `docs/todo-stage12-release.md`. Commit/push/deploy не выполнялись.

### 17.09.2026 — этап 11, завершение оставшихся блоков (в работе)

**Полностью закрыт PICK-04: 30/45, осталось 15. Остальные перечисленные изменения частичные; весь TODO ещё не завершён.**

- **Итог проверок этапа:** 158 файлов / 767 тестов прошли; backend TypeScript, production-сборка, бюджет стартового JS и git diff --check прошли. Строгая frontend-проверка не проходит: 300 диагностик.

- Выполняемый маршрут нельзя удалить. Дополнительная проверка не позволяет удалить маршрут с прибытием, забранным грузом или нерешённой проблемой даже при некорректном статусе маршрута. Сведения об удалённом завершённом маршруте сохраняются в событиях.
- API-03: постоянный атомарный захват отправки в 1С, сравнение содержимого, возврат успешной квитанции, блокировка повтора при неизвестном результате. Остаются сверка с реальной 1С и интерфейс восстановления.
- API-06: AbortSignal для 2ГИС/отправки в 1С на Node-сервере и защита позднего ответа. Остальные внешние вызовы и serverless не покрыты полностью.
- SEC-08/PICK-03: срок снимка 7 суток, предупреждение о старом черновике, подтверждение очистки неотправленной работы при выходе/удалении аккаунта, обработка abort хранилища. Координаты старше 7 суток скрываются, очистка выполняется порциями. Нужны межвкладочная проверка, плановая серверная очистка и физические устройства.
- PICK-05: монитор предупреждает о работе GPS только на открытом экране. Фоновое отслеживание пока не реализовано.
- TECH-01: добавлен отдельный строгий frontend typecheck. Исправлены типы деталей претензии, пути типов аналитики, параметры Flex и PDF, начальный набор прав API, ошибка обработки отказа истории push. Диагностик стало **300 вместо 369**; пункт остаётся открытым.
- TECH-04: общий React/clsx выделен из чанка графиков, возвраты загружаются лениво. Стартовый статический JS-граф — около **204 КБ gzip вместо 318 КБ** в промежуточной сборке. Добавлен бюджет сборки и его CI-проверка. Слабое устройство/мобильная сеть не измерялись.
- Условия выпуска, новые миграции 112–113 и ограничения: `docs/todo-stage11-release.md`. Секреты из env, рабочая база и реальные интеграции не использовались. Commit/push/deploy не выполнялись.

### 17.09.2026 — этап 10, ещё десять пунктов

**Обработаны 10 пунктов. Закрыты 8: SEC-09, API-08, PICK-09, PICK-10, PICK-11, UX-04, UX-05, TECH-05. UX-02 и TECH-03 реализованы в коде, но остаются открытыми до внешних проверок. Итого 29/45 закрыто, осталось 16.**

- Полный прогон: **154 файла, 759 тестов прошли**. Серверный TypeScript и production-сборка проходят. Дополнительная проверка frontend всё ещё выявляет существующие ошибки типов, в том числе Max UI props в NewOrderModal и unknown в сводке Wildberries; TECH-01 остаётся открытым. Размер большого чанка — TECH-04.
- Проверки очереди сохраняют фото и requestId при неизвестном результате, требуют согласия при смене версии и исключают перенос очереди между аккаунтами. Компоненты просмотрены на изолированной тестовой странице во встроенном браузере.
- Playwright E2E не прошёл инфраструктурный старт: установленный Chromium блокируется macOS sandbox. Физические Android/iOS и GitHub CI не проверены, обязательный check ветки не настроен. UX-02/TECH-03 поэтому не закрыты.
- **Перед выпуском SEC-09 применить миграцию 111 и настроить TRUSTED_PROXY_IPS.** Без таблицы защищённые endpoints будут возвращать 429; без корректного proxy IP пользователи могут разделять один лимит. Подробности: `docs/todo-stage10-release.md`.
- Рабочая БД, реальные 1С/2ГИС и env не использовались. Commit/push/deploy не выполнялись.

### 17.09.2026 — этап 9, десять пунктов с локальной проверкой

**Закрыты API-04, API-05, PICK-06, PICK-08, PICK-12, PICK-14, PICK-15, UX-03, UX-06, TECH-06. Итого 21/45, осталось 24 пункта.**

- Набор согласован с пользователем: десять пунктов, проверяемых локально; миграция MFA/сессий и аудит production-привязок не входят в этот этап и остаются открытыми.
- Реализация и проверки описаны у каждого пункта ниже. Для проверки реальных React-переходов добавлена dev-зависимость react-test-renderer 18.3.1; зависимости приложения не обновлялись.
- Общий прогон: **145 файлов, 742 теста прошли**. Общий TypeScript и production-сборка проходят; проверка всего frontend по-прежнему остаётся в TECH-01. Предупреждение о размере чанков остаётся в TECH-04.
- В build-артефакте проверен build-info.json с уникальным ID и временем. После общего прогона добавлена запись события ручного отказа от GPS и отдельный регрессионный тест этой операции; повторный целевой прогон API pickup и загрузки возвратов — **33 теста в 2 файлах прошли**.
- Проверки выполнены на изолированных данных, PGlite и React renderer. Физические устройства, рабочая база, реальные 1С/2ГИС и визуальная проверка всех экранов не выполнялись. Эти границы не снимают пункты ручного плана в конце файла.
- Изменения локальные, commit/push/deploy не выполнялись.

### 17.09.2026 — этап 8, версия черновика и таймаут обновлений

**Закрыт PICK-13; итого 11/45, осталось 34 пункта (включая частично выполненные).**

- Исправлен обнаруженный при сверке с pull побочный дефект PICK-01: при первом вводе пустой черновик принимает текущую видимую версию задания. После собственного «Прибыл» не возникает ложного сообщения об изменении диспетчером. Черновик с местами, комментарием или фото сохраняет исходную версию и требует явного подтверждения, если задание изменилось.
- Добавлено пять проверок переходов версии: прибытие → ввод мест, ввод до эффекта смены шага, сохранение версии при введённом нуле, комментарии и фото; явное подтверждение новой версии остаётся доступным.
- Запрос манифеста обновления ограничен 10 секундами, включая чтение тела ответа. Web-запрос отменяется через AbortController; для native заданы connect/read timeout и отдельный предел ожидания ответа моста. Поздний ответ не меняет уже возвращённый результат. При недоступности сервера профиль показывает существующее сообщение об ошибке и позволяет повторить проверку.
- Четыре теста проверяют зависший web-запрос, зависшее тело ответа, native-мост/поздний ответ, повтор после таймаута и очистку таймера. Всего в целевом прогоне **19 тестов прошли**, общий `tsc --noEmit` и production-сборка проходят. Предупреждение о размере чанков остаётся. Физические Android/iOS-устройства не проверялись.
- Изменения локальные, commit/push/deploy не выполнялись.

### 17.09.2026 — проверка совместимости с pull `4cb7871d`

- Проверены пересечения в шести файлах pickup: API, формы, экран водителя, контейнер маршрута, страница и операции. Новые номера `ZB-…`, возможность продолжения/сдачи без решения диспетчера и версии Android/iOS сохранены. Прямого противоречия локальных TODO-исправлений этим изменениям не найдено.
- **Найден побочный дефект локального PICK-01 (исправлен на этапе 8):** после `arrive` сервер увеличивал `job.version`, а черновик сохранял исходную версию. Первый ввод количества включал ложное предупреждение «Диспетчер обновил точку». Это не текстовый конфликт merge; исходные тесты хранилища переход версии не покрывали. Исправление и дополнительные проверки описаны выше.
- Перед совместным выпуском остаётся ранее указанное требование SEC-03: старые установленные клиенты не передают пароль в запросах компаний. Нужен согласованный выпуск backend и обновлённых клиентов. Новая миграция `110_pickup_job_number.sql` из pull обязательна для создания заборов; применение на рабочей базе в этой проверке не подтверждалось.
- В рамках проверки код приложения не менялся, конфликты вручную не разрешались.

### 17.09.2026 — этап 7, ошибки загрузки заявок и query-параметры

**Закрыт API-07; итого 10/45. API-04 выполнен частично.**

- После обновления до `4cb7871d` сохранены локальные исправления предыдущих этапов.
- Ошибки БД, отсутствие снимка кэша и некорректный формат кэша больше не превращаются в успешный пустой список заявок. Основной и партнёрский API возвращают 503 с понятным сообщением, кодом `ORDERS_UNAVAILABLE` и request ID без внутренних деталей ошибки.
- Ошибка чтения ожидающих заявок больше не скрывается за частично успешной выдачей. Настоящий пустой снимок по-прежнему возвращает пустой список. Существующий UI обрабатывает неуспешный HTTP-ответ как ошибку, отдельно от состояния «нет заявок».
- API-04 остаётся открытым в части отображения времени синхронизации и предупреждения при выдаче старого кэша; текущий fallback на старый снимок сохранён.
- Серверный адаптер сохраняет повторяющиеся query-параметры как массивы в исходном порядке; одиночные параметры остаются строками. Объект без прототипа корректно обрабатывает служебные имена ключей.
- Добавлено 9 регрессионных тестов. Полный прогон после pull: **139 файлов, 715 тестов — успешно**. Общий `tsc --noEmit` и production-сборка проходят; typecheck пока не покрывает весь frontend (TECH-01), предупреждение о размере чанков остаётся (TECH-04).
- Изменения локальные; commit/push/deploy не выполнялись.

### 17.09.2026 — этап 6, смены водителей и доступность автомобилей

**Закрыт PICK-07; итого 9/45.**

- В справочник водителей добавлены начало и окончание смены, в справочник автомобилей — период доступности. Новые записи получают 08:00–18:00 по умолчанию; существующие записи без графика остаются совместимыми.
- API сохраняет и проверяет оба времени. Частично заполненный период, равные границы и ночная смена отклоняются с явным сообщением: ночные смены пока не поддерживаются.
- Проверка маршрута больше не подменяет неизвестный график подтверждённой доступностью на весь день: для отсутствующего графика выводится предупреждение, а некорректный график блокирует оценку. Публикация также сообщает о некорректной смене водителя.
- Проверки: тесты смен, маршрутизации и операций прошли; полный результат **136 файлов и 703 теста**, TypeScript и production-сборка проходят.
- Многодневные ночные рейсы остаются отдельным будущим расширением; текущий продуктовый контракт явно ограничен сменой внутри календарного дня.
- Изменения локальные; commit/push/deploy не выполнялись.

### 17.09.2026 — этап 5, типизация и стабильность форматов

**Дополнительно закрыты API-09 и TECH-02; итого 8/45. TECH-01 выполнен частично.**

- Все 13 диагностик исходного общего `tsc --noEmit` исправлены. Проверка теперь завершается успешно: добавлены проверки nullable admin-token payload, нормализация входных push-шаблонов, корректные type/import-контракты и безопасное отсутствие `mainlineOptions`.
- Время 1С без явного timezone теперь интерпретируется как стабильное бизнес-время без зависимости от TZ процесса; группировка дня и отображение часов используют тот же UTC-контракт. Тест отчёта последней мили проходит.
- Формат суммы в ежедневном уведомлении нормализован до обычного пробела, чтобы текст был одинаковым в Node/ICU-окружениях. Проверка содержимого сохранена.
- Полный результат: **136 файлов и 702 теста прошли**, общий TypeScript проходит, production-сборка проходит. Предупреждение о крупном bundle остаётся в TECH-04.
- TECH-01 остаётся частично открытым: исправлены все известные диагностики общего tsconfig, но весь frontend ещё не включён в единый обязательный typecheck/script перед merge.
- Изменения локальные; commit/push/deploy не выполнялись.

### 16.09.2026 — этап 4, сохранность ввода водителя

**Дополнительно исправлены в коде PICK-01 и PICK-02; итого 6/45. PICK-03 выполнен частично.**

- Убрано обнуление мест, комментария и фотографий при изменении version. При несовпадении сохранённой и текущей версии показывается предупреждение; действия блокируются до явного подтверждения водителем новых данных.
- Водительский сценарий и нижняя навигация больше не зависят от порога 768px. Поворот/широкий экран не переключают форму на другое дерево компонентов; диспетчерский режим этим условием не изменён.
- Добавлен IndexedDB-черновик, изолированный по логину и ID задания: количество, комментарий, фото, шаг и версия. Восстановление завершается до доступности ввода. Статус «сохраняем/сохранён» отражает запись на диск; при ошибке хранилища есть предупреждение.
- Записи сериализованы по ключу: позднее сохранение фото не восстанавливает уже удалённый черновик. После подтверждённой сервером или сохранённой в outbox отметки черновик очищается. Есть отдельное удаление с подтверждением; при неуспешной отправке черновик сохраняется.
- Количество по плану теперь заполняется явной кнопкой: очистка поля пользователем больше не вызывает автоматическое повторное заполнение.
- **Проверки:** 4 новых теста хранилища прошли; все **123 теста pickup в 34 файлах прошли**. Сборка проходит. В браузере на изолированном локальном стенде подтверждены сохранение комментария при смене версии, блокировка действий до подтверждения и восстановление шага/комментария после reload. Персистентность массива фото проверена unit-тестом; загрузка фото камерой и реальное убийство процесса ОС не проверялись.
- Проверка типов затронутого pickup не выявила ошибок новых файлов; остаются прежние ошибки импортируемого NewOrderModal и объявления глобальных platform/env-типов при отдельном запуске. TECH-01 остаётся открытым.
- **PICK-03 ещё открыт:** нужен согласованный срок хранения/очистки неотправленных фото (связан с SEC-08), проверка квоты на устройствах и восстановления после выгрузки приложения ОС. Не гарантируется сохранность фото, пока его обработка/запись ещё не завершены; это отображается статусом сохранения.
- Проверка на физических устройствах и визуальная проверка полного водительского экрана на планшете остаются в плане верификации. Все изменения локальные, без commit/push/deploy.

### 16.09.2026 — этап 3, защита компаний (SEC-03, код исправлен)

- Чтение `/api/companies` требует подтверждения каждого аккаунта. POST передаёт список учётных данных в теле; GET поддерживает только одну авторизованную учётную запись через заголовки и отклоняет запрос чужого логина. Sandbox сохраняет отдельную проверку прав.
- `/api/companies-save` больше не принимает присланные компании как источник привязок. Для зарегистрированного пользователя изменения доступны через админку; login-time sync не переписывает их даже при правильном пароле. Для legacy сервер получает список в 1С с учётными данными самого пользователя.
- Для API v1 сохранён fallback к GetPerevozki: сервер самостоятельно получает первую компанию из успешно авторизованного ответа, как прежде делал клиент. Нет подтверждённой компании — нет изменения привязок. Ошибка/пустой ответ не удаляет существующие записи.
- Обновлены клиент списка компаний, переключатель, список аккаунтов, синхронизация заказчика и оба сценария входа. Пароли не передаются в URL. Неверный пароль существующей зарегистрированной учётки не запускает fallback в legacy.
- **Проверки:** 9 новых серверных и 2 клиентских теста прошли; тесты транзакции адаптированы к новому контракту и проходят. Общий прогон **696/698**, только два прежних сбоя. Сборка проходит. Общий TypeScript — прежние 6 диагностик; сравнительная проверка затронутого frontend не выявила новых диагностик относительно HEAD (полная типизация frontend остаётся долгом TECH-01).
- **Ограничение совместимости:** старые клиенты без пароля в этих запросах получат отказ; backend и frontend требуется выпустить согласованно. Для legacy чтение/синхронизация компаний теперь зависит от доступности 1С. Реальная 1С и native-устройства не проверялись.
- **Не завершено:** сверка ранее сохранённых `account_companies` на рабочей базе с назначениями админов/данными 1С. Старые привязки не удалялись автоматически: среди дополнительных ИНН могут быть законные назначения. До этой сверки SEC-03 и зависимый SEC-04 остаются отмечены как частично завершённые, хотя новые неавторизованные изменения закрыты тестами. Серверный MFA (SEC-01/02) остаётся отдельной открытой задачей.
- Изменения локальные; commit/push/deploy не выполнялись.

### 16.09.2026 — этап 2, защита выдачи заказов (SEC-04 частично)

- Запрошенные ИНН и название больше не обходят разрешённые компании. Пустой список прав не превращается в доступ ко всем заказам.
- `serviceMode: true` даёт расширенную выдачу только при серверном `permissions.service_mode === true`; остальные запросы фильтруются обычным способом. Доступ зарегистрированного пользователя как разрешённого отправителя сохранён.
- Общий кэш больше не выдаётся legacy-пользователям до проверки личности: такие запросы идут в 1С с их учётными данными. **Компромисс:** для этого типа входа получение заявок теперь зависит от доступности/скорости 1С; безопасный персональный кэш ещё не реализован.
- Ожидающие заявки используют тот же расчёт разрешённых ИНН; совпадение названия не отменяет ограничение.
- Ошибка разбора/фильтрации внешнего ответа возвращает ошибку, а не исходный необработанный ответ 1С.
- Добавлено **7 тестов** на чужой ИНН/название, поддельный serviceMode, пустые права, допустимые роли, отправителя, pending-заявки, legacy-авторизацию и некорректный внешний ответ. Все прошли. Общий прогон до добавления последнего теста: **684/686**, только два прежних сбоя; последний тест проверен отдельным прогоном файла (7/7). TypeScript: прежние 6 диагностик, новых нет.
- **SEC-04 остаётся открытым:** `account_companies` по-прежнему используется как источник разрешений, а SEC-03 ещё не исправлен. Защита этого источника и проверка существующих привязок обязательны для полного закрытия. SEC-01/02 также не исправлены. Итого полностью закрыты по-прежнему 4/45 пунктов; один дополнительно выполнен частично.
- Изменения обоих этапов локальные, commit/push/deploy в этих этапах не выполнялись.

### 16.09.2026 — этап 1, локальные исправления

**Исправлено 4 из 45 пунктов:** SEC-05, SEC-07, API-01, API-02. **41 пункт остаётся открытым**, включая критичные SEC-01–04. Этот этап не закрывает серверный обход MFA и изоляцию компаний/заказов.

- Добавлены 9 регрессионных тестов в 4 файлах; все прошли.
- Общий прогон: **678 из 680 тестов прошли**. Остались те же два исходных сбоя: API-09 (дата) и TECH-02 (неразрывный пробел); новых падений нет.
- Сборка проходит. TypeScript: **6 диагностик вместо 13**, оставшиеся относятся к TECH-01.
- Локальные env-файлы и реальные секреты не читались; deploy не выполнялся.
- Следующий этап: SEC-01–04. Требуются согласованные изменения серверного подтверждения личности, клиентов 2FA, назначения компаний и фильтрации кэша/прямой выдачи заказов. Нельзя считать их закрытыми одной проверкой пароля или удалением UI-кнопок.

## Главное

В первую очередь закрыть серверную авторизацию: изменение 2FA без проверки пользователя, назначение компаний без авторизации и обход ограничения заказов по заказчику. Интерфейсная проверка прав не защищает API. Затем устранить потерю введённых водителем данных, ошибки партнёрского API и неоднозначные состояния операций.

### Обозначения

- **P0** — критичная граница доступа; исправить до следующего выпуска с затронутыми API.
- **P1** — высокий риск потери данных, нарушения сценария или недоступности функции.
- **P2** — плановое улучшение надёжности, продукта, интерфейса или технического долга.
- **Воспроизведено** — дефект подтверждён автоматической проверкой с изолированными зависимостями.
- **По коду** — подтверждена реализация; полный сценарий на рабочем сервере/устройстве не выполнялся.
- **Риск / продуктовый долг** — требуется согласовать поведение или проверить интеграционные условия. Это не утверждение о произошедшем инциденте.

## Объём и ограничения

Проверены серверная маршрутизация и адаптер, авторизация и компании, получение заказов и партнёрский API, отправка в 1С, диспетчеризация и водитель, GPS, офлайн-очередь, обновления, отдельные сценарии Wildberries и возвратов, финансовые UI-компоненты, общие модальные окна, сборка и тестовая инфраструктура. Глубина проверки выше у авторизации, заказов и pickup; остальные крупные разделы проверены выборочно по коду. Это не полная ручная проверка каждого экрана.

`.env` и реальные API-ключи не читались. Рабочая база, Redis, 1С и внешние сервисы не изменялись. Не выполнялись установка APK, проверка на физических Android/iOS, измерение контрастности, Lighthouse или нагрузочное тестирование. Локальный pickup-preview с подменённым API не доказывает работоспособность production-интеграций.

### Проверки

Проверки запускались в рабочей копии с установленными зависимостями, соответствующей проверенному коду; исходный репозиторий не использовался для переустановки зависимостей.

| Проверка | Результат |
| --- | --- |
| `npm test` | 128 файлов: 126 прошли, 2 упали; 669 из 671 теста прошли |
| `npm run build` | Успешно; предупреждения о чанках больше 500 kB |
| `tsc --noEmit` | 13 диагностик; общий TypeScript-контроль не проходит |
| Дополнительные изолированные проверки безопасности | 5 из 5 подтвердили описанные дефекты |

Дополнительные проверки использовали фиктивные логины и моки DB/Redis: запись настроек 2FA без авторизации; создание Google 2FA без авторизации; запись компаний без авторизации; обход фильтра компаний при выдаче заказов; работоспособность подписи admin-токена без настроенного секрета. Внешние запросы с целью эксплуатации не выполнялись. Временный файл этих проверок не добавлен в приложение.

---

## 1. Авторизация, изоляция пользователей и хранение данных

### SEC-01 · P0 · Изменение 2FA без авторизации

- [x] Закрыть неавторизованное чтение и изменение настроек второго фактора.
- **Статус:** закрыт локально на этапе 12; интеграционная проверка выпуска остаётся обязательной.
- **Исправлено:** анонимное чтение отклоняется; запись/выдача нового секрета требуют сессии настройки с проверкой владельца, повторного пароля и действующего OTP. Настройка staged, активация атомарна. Старый фактор не перезаписывается до проверки нового. QR локальный, ошибки не скрываются. Защищены также alias отвязки и перепривязка активного Telegram-фактора в боте.
- **Где:** `api/2fa.ts`, `api/2fa-google.ts`; общий диспетчер `server/index.ts` не добавляет проверку личности перед вызовом этих обработчиков.
- **Проблема:** достаточно передать логин. Можно менять настройки, создавать новый секрет и отключать Google 2FA. `setup` сразу перезаписывает рабочий секрет до подтверждения кода.
- **Последствие:** посторонний может нарушить или ослабить защиту другого аккаунта. Это не означает, что одного логина достаточно для прохождения первого фактора.
- **Сделать:** получать пользователя из серверной сессии; проверять владельца; для замены/отключения требовать повторное подтверждение личности. Хранить новый секрет как ожидающий подтверждения до корректного OTP.
- **Готово, когда:** анонимный и чужой пользователь получают 401/403; неудачная настройка не ломает действующий фактор; разрешённая настройка проходит сквозной тест.

### SEC-02 · P0 · Второй фактор не связан с серверным доступом к данным

- [ ] Перенести завершение MFA в серверный протокол авторизации.
- **Статус:** по коду.
- **Где:** `api/auth-registered-login.ts`, `lib/verifyRegisteredUser.ts`, `src/components/LoginScreen.tsx`.
- **Проблема:** UI выполняет отдельный этап 2FA, но проверка пользователя для защищённых API основывается на логине и пароле, без серверного доказательства завершённого второго фактора.
- **Последствие:** клиентский экран можно обойти прямым обращением к API с первым фактором.
- **Сделать:** после пароля выдавать ограниченный challenge; после MFA — полноценную сессию. Проверять её в защищённых API.
- **Готово, когда:** при включённом MFA одного правильного пароля недостаточно для чтения защищённых данных; challenge нельзя использовать как полноценную сессию.

### SEC-03 · P0 · Справочник компаний доступен для записи без проверки пользователя

- [ ] Защитить чтение компаний и изменение привязок аккаунт–ИНН.
- **Прогресс 16.09.2026:** код защиты чтения/записи и клиенты исправлены, 11 новых тестов прошли (см. этап 3). Осталась сверка исторических привязок и интеграционная проверка выпуска.
- [x] Требовать подтверждения личности при чтении и записи; игнорировать присланные назначения компаний.
- [x] Обновить все обычные клиентские вызовы списка/синхронизации компаний.
- [ ] Сверить существующие привязки на рабочей базе, затем проверить согласованный выпуск backend/frontend.

- **Статус:** запись воспроизведена; чтение и использование привязок подтверждены по коду.
- **Где:** `api/companies-save.ts:24`, `api/companies.ts:40`, `api/orders/index.ts`.
- **Проблема:** запись принимает `login` и `customers` без проверки личности и заменяет `account_companies`. Обычное чтение тоже опирается на переданный логин. Эти привязки далее участвуют в ограничении доступных заказов.
- **Последствие:** изменение чужих привязок и нарушение изоляции компаний. Проверка флага `access_all` у указанного логина не доказывает, что запрос сделал его владелец.
- **Сделать:** определять пользователя сервером; разрешённые компании получать из доверенного источника, не назначать произвольные ИНН из клиента. Проверить целостность существующих привязок после исправления.
- **Готово, когда:** нельзя читать или менять чужой список, а обычный пользователь не может добавить произвольную компанию и получить её данные.

### SEC-04 · P0 · Обход фильтра заказов по заказчику

- [ ] Применять серверные ограничения ко всем веткам выдачи заказов, включая кэш.
- **Прогресс 16.09.2026: частично выполнено.** См. этап 2 журнала: закрыты обходы фильтра и выдача legacy-кэша до авторизации, добавлены регрессионные тесты. Код SEC-03 теперь исправлен (этап 3); для полного закрытия остаётся проверка доверенности существующих привязок и интеграционная проверка.
- **Статус:** обход внутри `filterRegisteredOrdersList` воспроизведён изолированным тестом.
- **Где:** `api/orders/index.ts:104–157`, обработчик зарегистрированных пользователей и legacy-кэша.
- **Проблема:** совпадение переданного `inn`/`customerName` возвращается до проверки принадлежности; `serviceMode` доверяется как входному флагу. При отсутствии разрешённых ИНН возможна ветка без ограничения. Legacy-кэш выдаётся до проверки переданного пароля во внешнем источнике, если запись кэша найдена.
- **Последствие:** выдача данных вне разрешённых компаний; для legacy-ветки риск зависит от наличия записи кэша.
- **Сделать:** авторизация до чтения кэша; права service mode — только из серверной роли; запрошенный набор пересекать с разрешённым; пустой набор прав должен означать отсутствие доступа.
- **Готово, когда:** отрицательные тесты закрывают чужой ИНН, имя компании, подставленный serviceMode, отсутствие привязок и неверный пароль при существующем кэше.

### SEC-05 · P1 · Предсказуемый запасной секрет admin-токенов

- [x] Запретить запуск подписи/проверки admin-токенов без заданного секрета.
- **Выполнено 16.09.2026:** Удалён публичный fallback: без ADMIN_TOKEN_SECRET создание токена запрещено, проверка возвращает отказ. Тесты покрывают отсутствие/пустое значение, штатную подпись, подмену и ротацию. **Перед выпуском:** проверить наличие непустого секрета во всех серверных окружениях; значение не читалось. Если ранее использовался fallback, требуется ротация. Это эксплуатационная проверка, не выполненная локально.
- **Статус:** воспроизведено при удалённой из тестового окружения переменной; наличие проблемы в production не проверялось.
- **Где:** `lib/adminAuth.ts:7`.
- **Проблема:** при отсутствии `ADMIN_TOKEN_SECRET` используется константа из исходников.
- **Сделать:** обязательная валидация окружения, отказ в обслуживании admin-auth при отсутствии секрета; при обнаружении использования fallback — ротация и отзыв прежних токенов.
- **Готово, когда:** тест с отсутствующим секретом не может создать или принять токен; секрет не имеет публичного значения по умолчанию.

### SEC-06 · P1 · Пароли аккаунтов сохраняются в localStorage

- [ ] Исключить постоянное хранение паролей в браузерном состоянии.
- **Статус:** по коду.
- **Где:** `src/contexts/AuthContext.tsx:63`, `src/contexts/AuthContext.tsx:142`, `src/types.ts`, `src/features/pickup/client.ts`.
- **Проблема:** сериализуется аккаунт с паролем, который затем используется в API-запросах. XSS здесь не обнаружена, но любое выполнение чужого JS в origin получает доступ к сохранённым паролям.
- **Сделать:** серверные сессии; для web — подходящий защищённый cookie-протокол, для native — защищённое хранилище токенов. Миграция должна удалить старые сохранённые пароли.
- **Готово, когда:** после входа и перезагрузки в localStorage/IndexedDB нет пароля; logout отзывает сессию.

### SEC-07 · P1 · Удаление последнего аккаунта оставляет сохранённую авторизацию

- [x] Очищать сохранённые аккаунты при удалении последнего аккаунта.
- **Выполнено 16.09.2026:** Сохранение пустого набора очищает все четыре auth-ключа и обновляет внутрипроцессный кэш восстановления. Убрана дублирующая миграция из effect: её уже выполняет getInitialAuthState. Два теста проверяют удаление последнего аккаунта с перезагрузкой модуля и сохранность миграции legacy-аккаунта. Проверки UI на физическом устройстве ещё не выполнялись.
- **Статус:** по коду.
- **Где:** `src/hooks/useAccountActions.ts` → `handleRemoveAccount`; `src/contexts/AuthContext.tsx:140`.
- **Проблема:** удаление выставляет пустой массив, а сохраняющий effect выходит при `accounts.length === 0`. Старое значение остаётся и может восстановиться после перезагрузки. Обычный logout имеет отдельную очистку — замечание касается именно удаления аккаунта.
- **Готово, когда:** удалить единственный аккаунт → перезапустить → аккаунт не восстанавливается; выбор активных аккаунтов тоже очищен.

### SEC-08 · P2 · Не определён жизненный цикл локальных данных и координат

**Этап 11, частично:** срок кэша, подтверждение выхода/удаления аккаунта при неотправленной работе, очистка собственных записей и ограниченная очистка GPS реализованы. Плановый cleanup, несколько вкладок и устройства ещё не проверены.

- [ ] Добавить политику очистки pickup-кэша, очереди и устаревших координат.
- **Статус:** по коду / продуктовый долг.
- **Где:** `src/hooks/useAppLogout.ts`, `src/features/pickup/client.ts` (IndexedDB `haulz-pickup`), `migrations/108_pickup_driver_locations.sql`.
- **Проблема:** logout очищает несколько auth-ключей, но сохраняет pickup-кэш и очередь; адреса, контакты и фото могут оставаться на общем устройстве. Для координат нет явного прикладного срока хранения.
- **Сделать:** изоляция по пользователю, срок хранения, безопасная очистка. Перед удалением несинхронизированных фото/отметок предусмотреть отправку или восстановление — нельзя просто уничтожить очередь.
- **Готово, когда:** новый пользователь устройства не получает данные предыдущего; истёкшие записи удаляются; несинхронизированная работа не теряется молча.

### SEC-09 · P2 · Ограничение частоты запросов работает только внутри процесса

- [x] Перевести чувствительные лимиты на общее атомарное хранилище.
- **Статус:** закрыт локально; перед выпуском обязательны миграция и proxy-конфигурация.
- **Этап 10, 17.09.2026:** Общий атомарный счётчик PostgreSQL заменил Map; добавлена миграция 111. Ключи хешируются, просроченные записи очищаются ограниченными порциями. X-Forwarded-For учитывается только от TRUSTED_PROXY_IPS; при отказе хранилища запрос блокируется. PGlite проверяет конкурентные обращения, сброс окна и отказ БД; отдельный тест проверяет подделку forwarding-заголовка. Требования выпуска: docs/todo-stage10-release.md. Реальные воркеры production не запускались.
- **Где:** `lib/rateLimit.ts`.
- **Проблема:** Map не разделяется между воркерами и сбрасывается при рестарте. Нужно также явно задать доверенные proxy-заголовки для определения IP.
- **Готово, когда:** один лимит действует при запросах к двум воркерам; клиент не обходит его подстановкой заголовка; очистка лимитов не создаёт существенную нагрузку.

## 2. API, целостность данных и интеграции

### API-01 · P1 · Неразрешимые импорты партнёрского API заказов

- [x] Исправить пути импортов и добавить проверку загрузки обработчиков.
- **Выполнено 16.09.2026:** Исправлены семь путей импортов. Smoke-тесты действительно импортируют оба обработчика и проверяют отказ запроса без авторизации с подменённым auth-сервисом. Семь соответствующих ошибок TypeScript исчезли. Рабочая 1С в проверке не вызывалась.
- **Статус:** воспроизведено TypeScript-проверкой.
- **Где:** `api/partner/v1/orders.ts:7`, `api/partner/v1/orders/create.ts:2–10`.
- **Проблема:** `../../orders.js` не соответствует `api/orders/index.ts`; в create неверная глубина путей к `_lib` и `lib`. Обработчик может не загрузиться до выполнения бизнес-логики.
- **Готово, когда:** оба обработчика импортируются в целевом серверном окружении; smoke-тест вызывает их с подменёнными интеграциями, а не только проверяет наличие имени в реестре.

### API-02 · P1 · Нет ROLLBACK при ошибке сохранения компаний

- [x] Добавить корректный rollback транзакции.
- **Выполнено 16.09.2026:** Добавлен ROLLBACK при ошибке; если сам откат не удался, соединение удаляется из пула через release(error). Три теста с подменённым клиентом БД проверяют порядок rollback/release, удаление сломанного соединения и успешный COMMIT. SEC-03 (авторизация этого endpoint) остаётся открытым.
- **Статус:** по коду.
- **Где:** `api/companies-save.ts:67–82`.
- **Проблема:** после BEGIN выполняются DELETE/INSERT; при ошибке клиент освобождается без ROLLBACK. Соединение может вернуться в пул с незавершённой/ошибочной транзакцией.
- **Готово, когда:** искусственная ошибка INSERT сохраняет исходные привязки, откатывает транзакцию и не ломает следующий запрос на том же соединении.

### API-03 · P1 · Риск повторной отправки заявки в 1С

**Этап 11, частично:** миграция 112 и submitOnce защищают от конкурентного повтора и повторной отправки после неизвестного результата. Есть тесты конкуренции, сетевого сбоя и сбоя БД после успеха. Нет подтверждённого API сверки/интерфейса восстановления; не закрыт.

- [ ] Сделать отправку устойчивой к параллельным вызовам и неопределённому результату сети.
- **Статус:** риск по коду; гарантии идемпотентности самой 1С не проверялись.
- **Где:** `lib/documentsOrderSubmitPending1c.ts:37–89`, `api/haulz-calculator/submit-pending-1c.ts`, `api/haulz-calculator/draft-status.ts`.
- **Проблема:** чтение согласованной заявки → внешняя отправка → локальная смена статуса, без атомарного захвата. Два вызова могут отправить её одновременно; после успеха в 1С и сбоя локального обновления повтор тоже возможен.
- **Сделать:** атомарный переход в состояние отправки, постоянный ключ идемпотентности, восстановление неопределённого результата через сверку с 1С.
- **Готово, когда:** параллельные вызовы и сбой после внешнего успеха не создают две заявки; пользователь видит состояние ожидания/ошибки/успеха.

### API-04 · P1 · Ошибка получения заказов маскируется пустым списком

- [x] Отличать отсутствие заказов от недоступности данных.
- **Выполнено 17.09.2026 (этап 9):** Кэш заявок передаёт время обновления и признак устаревания через opt-in withMetadata; прежний массив для старых клиентов сохранён. UI показывает предупреждение и дату синхронизации, отдельно от ошибки и пустого результата. Добавлен тест метаданных старого снимка.
- **Статус:** по коду.
- **Где:** `api/orders/index.ts`, `readRegisteredOrdersFromCache` и catch зарегистрированной ветки.
- **Проблема:** ошибки превращаются в `[]`/успешный ответ; fallback на старый кэш не даёт пользователю понятной оценки свежести.
- **Последствие:** пользователь воспринимает сбой как отсутствие перевозок.
- **Готово, когда:** отказ БД показывает ошибку/старые данные с временем синхронизации; настоящий пустой результат имеет отдельное состояние.

### API-05 · P1 · Сервер читает тело запроса без ограничения размера

- [x] Ограничить объём входящих запросов до накопления всего тела в памяти.
- **Выполнено 17.09.2026 (этап 9):** Поток JSON/обычного тела ограничен до буферизации: 4 MiB для pickup (вмещает 3 JPEG по 900 КБ в base64), 16 MiB для остальных маршрутов, 30 секунд чтения. Сервер возвращает 413/408 и закрывает соединение. Multipart по-прежнему читает собственный ограниченный парсер. Четыре теста проверяют Content-Length, chunked-превышение, фото и таймаут.
- **Статус:** по коду; ограничения внешнего reverse proxy не проверялись.
- **Где:** `server/vercelAdapter.ts` → `readRequestBody`.
- **Проблема:** все chunks собираются в память и объединяются до вызова обработчика и его авторизации.
- **Сделать:** лимиты по типу endpoint, ранний 413, ограничения времени чтения; допустимые фото водителя должны укладываться в выбранный контракт.
- **Готово, когда:** oversized-запрос прерывается без полного буферизования; штатная загрузка фото проходит.

### API-06 · P2 · Ответ 504 не прекращает выполнение операции

**Этап 11, частично:** контекст AbortSignal передаётся из Node-сервера в 2ГИС и отправку в 1С; адаптер игнорирует поздний ответ. Полное покрытие других интеграций и serverless остаётся.

- [ ] Согласовать серверный timeout с отменой и состояниями длительных операций.
- **Статус:** по коду / интеграционный риск.
- **Где:** `server/index.ts`, таймер 90 секунд.
- **Проблема:** клиент получает timeout, но обработчик может продолжить внешние запросы и изменения данных. Повтор пользователем может наложиться на первый вызов.
- **Готово, когда:** отменяемые операции получают AbortSignal; необратимые операции имеют идемпотентность и проверяемый статус; поздний ответ не пытается повторно завершить HTTP-response.

### API-07 · P2 · VPS-адаптер теряет повторяющиеся query-параметры

- [x] Сохранить контракт массивов query-параметров.
- **Выполнено 17.09.2026:** адаптер возвращает массив для повторяющегося ключа и строку для одиночного. Три теста проверяют порядок, декодирование, пустые значения и специальные имена ключей.
- **Статус:** по коду; использование такого запроса текущим UI не подтверждено.
- **Где:** `server/vercelAdapter.ts:39`, `api/companies.ts:40`.
- **Проблема:** `Object.fromEntries(url.searchParams.entries())` оставляет одно значение, хотя обработчики предусматривают массивы. Поведение отличается от ожидаемого Vercel-контракта.
- **Готово, когда:** повторяющиеся параметры дают одинаковый результат на обоих вариантах развёртывания либо массивы явно запрещены общим контрактом.

### API-08 · P2 · Все изменения pickup сериализованы одним глобальным lock

- [x] Сузить область блокировки до изменяемых сущностей.
- **Статус:** закрыт локально; нагрузочные замеры production не проводились.
- **Этап 10, 17.09.2026:** Команды выполнения используют shared gate и отдельную блокировку маршрута. Запуск захватывает также водителя и ТС в стабильном порядке; actor/requestId защищает повтор команд. Структурные изменения сохраняют exclusive gate. Контракт блокировок покрыт двумя тестами; интеграционные сценарии pickup на PGlite проходят.
- **Где:** `api/pickup/index.ts:1552`, `pg_advisory_xact_lock(104,1)`.
- **Проблема:** независимые изменения разных маршрутов/городов ждут одну блокировку.
- **Готово, когда:** разные маршруты изменяются параллельно, а конкурирующие изменения одного маршрута сохраняют согласованность; порядок захвата нескольких блокировок определён и проверен.

### API-09 · P1 · Неоднозначная интерпретация времени в отчёте последней мили

- [x] Зафиксировать часовой пояс входных дат и формат результата.
- **Выполнено 17.09.2026:** строки 1С без смещения разбираются детерминированно, а дата группировки и время отчёта используют тот же контракт. Тест проходит независимо от локального TZ процесса.
- **Статус:** падающий тест; ошибка реальных отчётов зависит от формата входных дат и TZ сервера.
- **Где:** `lib/lastMileVehicleReport.ts:44`, `lib/lastMileVehicleReport.test.ts:12`.
- **Проблема:** `new Date(text)` для строки без timezone зависит от окружения, а календарная дата извлекается локальными методами. Тест ожидает `2026-08-28T09:15:00`, получает `2026-08-28T06:15:00.000Z`.
- **Готово, когда:** установлен бизнес-контракт времени; одинаковые результаты при UTC и Europe/Moscow, включая границу суток. Не исправлять только expected, не выяснив смысл времени.

## 3. Диспетчеризация, водитель, маршрутизация и GPS

### PICK-01 · P1 · Обновление версии задания стирает ввод водителя

- [x] Сохранять черновик при фоновых обновлениях задания.
- **Выполнено 16.09.2026:** Убрано обнуление ввода по version, добавлен устойчивый черновик и явное подтверждение изменённой версии. Поведение комментария и шага подтверждено браузерной проверкой; запись фото — тестом хранилища. См. этап 4.
- **Статус:** по коду.
- **Где:** `src/features/pickup/PickupDriverJobFlow.tsx:51–57`, автообновление в `PickupPage.tsx`.
- **Проблема:** effect на `[job.id, job.status, job.version]` сбрасывает введённые места, комментарий и фото. Изменение диспетчером/порядка маршрута может увеличить version во время заполнения.
- **Готово, когда:** водитель выбирает фото, диспетчер меняет задание, приходит обновление — ввод сохранён; существенный конфликт показан явно с возможностью сверить изменения.

### PICK-02 · P1 · Поворот телефона меняет сценарий и уничтожает состояние формы

- [x] Отделить водительскую навигацию от ширины экрана.
- **Выполнено 16.09.2026:** Водительская навигация и форма теперь выбираются по роли/режиму, а не max-width:768px. Поворот не переключает дерево форм; профиль доступен и на широком экране. Ручная проверка полного сценария на физических устройствах остаётся в плане верификации.
- **Статус:** по коду; необходим регрессионный тест в браузере и на устройстве.
- **Где:** `src/hooks/useMobileLayout.ts`, `src/features/pickup/PickupPage.tsx` → `driverMobileUx`.
- **Проблема:** порог 768px переключает между разными деревьями компонентов. При переходе, например, 390→844px форма размонтируется; нижнее меню и профиль нового водительского интерфейса исчезают. То же касается планшета.
- **Готово, когда:** поворот/resize сохраняет ввод и выбранную точку; профиль, обновление и основные вкладки доступны водителю при любой ширине.

### PICK-03 · P1 · Незавершённая отметка не переживает закрытие приложения

**Этап 11, частично:** неотправленные фото не истекают молча; старый черновик отмечается, выход предлагает остаться для отправки. Проверка квоты/выгрузки ОС остаётся.

- [ ] Хранить черновики до отправки, включая подготовленные фотографии.
- **Прогресс 16.09.2026: частично выполнено.** Добавлены автосохранение в IndexedDB, восстановление, изоляция и ошибки записи; остаются сроки хранения и проверки на реальных устройствах. См. этап 4.

- **Статус:** по коду.
- **Где:** `src/features/pickup/PickupDriverJobFlow.tsx`, `src/features/pickup/client.ts`.
- **Проблема:** до попытки отправки данные живут только в React-state; outbox защищает уже поставленную в очередь операцию, но не незавершённый ввод.
- **Готово, когда:** выбрать фото и места → перезапустить/восстановить приложение после выгрузки ОС → продолжить черновик. Предусмотреть квоту хранилища, срок жизни и явное удаление черновика.

### PICK-04 · P1 · Удаление активного маршрута оставляет задания без рабочего маршрута

**Закрыт локально, этап 11:** запрет удаления начатого маршрута и любого маршрута с незавершённым грузом; сохранение метаданных удалённого маршрута в событиях. Проверки API и правил доступа обновлены.

- [x] Определить безопасный сценарий отмены/архивации выполняемого маршрута.
- **Статус:** подтверждённая логика, продуктовый риск; текущая политика удаления может быть намеренной.
- **Где:** `lib/pickup/model.ts:154`, `lib/pickup/deleteRoute.ts:60–74`.
- **Проблема:** политика допускает удаление маршрута любого статуса. Задания отвязываются, но сохраняют `arrived`/`picked_up`; связи событий с маршрутом обнуляются. Водитель лишается привычного пути завершения этих заборов.
- **Сделать:** для выполняемых маршрутов — перевод заданий с сохранением истории либо отмена/архив, а не физическое удаление без восстановления процесса.
- **Готово, когда:** после удаления/отмены рейса с забранным грузом есть явный ответственный водитель и путь сдачи на склад; история доступна.

### PICK-05 · P1 · Мониторинг GPS работает только при открытом экране

**Этап 11:** ограничение теперь явно показано диспетчеру. Это не реализация фоновой геолокации; пункт открыт.

- [ ] Привести обещание «где сейчас водитель» в соответствие с поведением платформ.
- **Статус:** по коду / продуктовый пробел.
- **Где:** `src/features/pickup/locationTracking.ts`.
- **Проблема:** обновления пропускаются при невидимом документе. Основной сценарий водителя — навигатор поверх приложения или заблокированный экран — прерывает обновления каждые 5 минут.
- **Сделать:** для native оценить фоновое отслеживание с явным разрешением и соблюдением ограничений ОС; для web показывать последнюю принятую позицию и её возраст, не обещать непрерывность.
- **Готово, когда:** проверены реальный Android/iOS, навигация в другом приложении и блокировка экрана; диспетчер видит актуальность координат и состояние отключённого отслеживания.

### PICK-06 · P2 · Первая правдоподобная ложная GPS-точка проходит фильтр

- [x] Добавить обработку сомнительного первого определения позиции.
- **Выполнено 17.09.2026 (этап 9):** Первая позиция вне района города помечается ненадёжной и не используется как старт маршрутизации; ошибочная первая точка не становится базой проверки скачков. Водитель может сообщить о неверной позиции и остановить передачу, событие видно диспетчеру. Эвристика не распознаёт правдоподобную подмену внутри района; полной защиты от GPS-спуфинга нет.
- **Статус:** ограничение текущей эвристики, не доказательство GPS-спуфинга устройства.
- **Где:** `src/features/pickup/locationTracking.ts` → `locationWarning`.
- **Проблема:** проверка скачка требует предыдущей точки; неверная первая координата с небольшой заявленной погрешностью может быть принята.
- **Сделать:** учитывать город/маршрут как сигнал сомнения, дать водителю сообщить «позиция неверна», показывать ориентир последней подтверждённой остановки отдельно от GPS. Не заменять GPS адресом без маркировки.
- **Готово, когда:** тесты покрывают неверную первую точку и правдоподобную погрешность; подозрительная точка не используется как надёжный старт расчёта. Не обещать полной защиты от подмены.

### PICK-07 · P2 · Ночная/некорректная смена превращается в полный день

- [x] Разделить отсутствие графика, ошибочный график и переход через полночь.
- **Выполнено 17.09.2026:** отсутствие графика обозначается предупреждением, некорректный/ночной график явно отклоняется, а дневная смена хранится в справочниках и участвует в расчёте. См. этап 6.
- **Статус:** по коду.
- **Где:** `lib/pickup/driverShift.ts`.
- **Проблема:** при `from >= to` возвращается интервал 00:00–23:59. Смена 22:00–06:00 теряет смысл, а проверка маршрута получает ложные ограничения.
- **Готово, когда:** ночная смена корректно поддерживается или явно отклоняется; незаполненный график обозначен как неизвестный, а не подтверждённая доступность весь день.

### PICK-08 · P2 · UI автоматически подтверждает временные окна

- [x] Не отправлять подтверждение окон без фактической проверки данных.
- **Выполнено 17.09.2026 (этап 9):** В диалоге показаны окна и текст часов отправителей. Подтверждение исходно выключено и требует действия диспетчера; изменение маршрута/точек/ресурсов сбрасывает его. Без подтверждения сервер возвращает неполную оценку. Проверен React-сценарий false → true → сброс после изменения версии.
- **Статус:** по коду.
- **Где:** `src/features/pickup/PickupRouteCheck.tsx:116`, `lib/pickup/checkRoute.ts`.
- **Проблема:** отправляется `windowsConfirmed: true`. Расчёт работает со структурированными окнами и не гарантирует интерпретацию произвольного текста часов склада.
- **Готово, когда:** неполные/текстовые часы дают статус «недостаточно данных» или требуют подтверждения структурированных окон; зелёная оценка не создаёт ложную уверенность.

### PICK-09 · P2 · Пробки следующих участков оцениваются на одно время выезда

- [x] Уточнять предложенный маршрут с учётом прогнозного времени каждого участка.
- **Статус:** закрыт локально; реальные платные запросы 2ГИС не выполнялись.
- **Этап 10, 17.09.2026:** Текущий и предложенный порядок уточняются по времени выезда с каждой точки после ожидания/погрузки. Для будущих участков используется статистика. Если уточнённое предложение стало хуже, оно не предлагается к применению. Сравнение сохраняет окна, смену и склад; UI показывает ограничения прогноза и геометрии карты. Тест подтверждает сдвиг времени следующего запроса на 80 минут с учётом открытия и погрузки.
- **Где:** `lib/pickup/checkRoute.ts`, построение матрицы с общим временем отправления.
- **Проблема:** участки, реально выполняемые спустя часы и после погрузок, получают одну исходную временную оценку. Это не доказательство неверности каждого маршрута, но ограничение заявлений об оптимальности.
- **Сделать:** повторно оценивать выбранный порядок по прогнозным временам прибытия/выезда; показывать время расчёта и допущения. Использовать формулировку «улучшений не найдено» вместо гарантии глобального оптимума.
- **Готово, когда:** окна, ожидание, погрузка, смена и конечный склад учитываются в итоговом сравнении; ограничения прогноза видны диспетчеру.

### PICK-10 · P2 · Начальный город водителя всегда Москва

- [x] Выбирать город по контексту водителя и сохранять осознанный выбор.
- **Статус:** закрыт локально.
- **Этап 10, 17.09.2026:** Начальный город берётся из карточки водителя с приоритетом его текущего опубликованного/начатого рейса. Ручной выбор сохраняется по аккаунту и имеет приоритет. Дата использует часовой пояс города. Тесты подтверждают передачу города в собственном профиле и изоляцию сохранённого выбора пользователей.
- **Где:** `src/features/pickup/PickupPage.tsx:146`.
- **Проблема:** `useState<City>("moscow")` не отражает назначение водителя Калининграда и сбрасывает выбор при новом запуске.
- **Готово, когда:** водитель Калининграда сразу видит свой текущий день; при нескольких городах выбор объясним и сохраняется.

### PICK-11 · P1 · Конфликт первой офлайн-операции блокирует оставшуюся очередь

- [x] Добавить доступное водителю разрешение конфликтов синхронизации.
- **Статус:** закрыт локально.
- **Этап 10, 17.09.2026:** Очередь доступна водителю: точка, дата/город новых отметок, причина ошибки, количество мест, комментарий и просмотр сохранённых фото. При 409 можно сверить актуальный статус и явно подтвердить новую версию с новым requestId, без изменения фото. Независимые точки отправляются; зависимые команды и неоднозначные сетевые ошибки остаются. Тесты покрывают 409, независимые задания, потерю сети, сбой записи, явное согласие и сохранение фото.
- **Где:** `src/features/pickup/PickupPage.tsx`, обработка outbox; `src/features/pickup/client.ts`.
- **Проблема:** очередь прекращает отправку при первой ошибке, включая 409 устаревшей версии. Обновление данных само по себе не исправляет версию сохранённой команды. Детальный список outbox скрыт в мобильном режиме водителя.
- **Готово, когда:** водитель видит проблемную точку и причину, может сверить и повторить/разрешить конфликт без потери фото; независимые операции не заблокированы навсегда. Нельзя молча менять версии и перезаписывать чужие изменения.

### PICK-12 · P2 · Обновление iOS представлено как обновление web

- [x] Разделить web-, Android- и iOS-сценарии обновления.
- **Выполнено 17.09.2026 (этап 9):** Водительский и общий профиль различают web, native Android и native iOS. iOS предлагает обновление через источник установки (TestFlight/App Store), без перезагрузки web-кода; Android сохраняет проверку APK, web сравнивает build ID. React-тест подтверждает отсутствие reload на iOS. Фактическая установка на устройствах не проверялась.
- **Статус:** по коду; установка на iOS не проверялась.
- **Где:** `src/features/pickup/PickupDriverNavigation.tsx` → `DriverProfile`, `src/lib/androidAppUpdate.ts`.
- **Проблема:** ветка «не native Android» включает iOS и предлагает «Обновить веб-версию» с перезагрузкой, которая не обновляет установленный native-бинарник.
- **Готово, когда:** профиль определяет платформу и предлагает соответствующий канал обновления; обновление не уничтожает несохранённую работу.

### PICK-13 · P2 · Проверка обновления может зависнуть

- [x] Ограничить время запроса манифеста обновления.
- **Выполнено 17.09.2026:** 10-секундный предел ожидания, отмена web-запроса и таймауты native HTTP. Тестами проверены зависание сети/тела/моста, повтор, поздний ответ и очистка таймеров. Проверка на физических устройствах остаётся в ручном плане.
- **Статус:** по коду.
- **Где:** `src/lib/androidAppUpdate.ts` → `fetchAndroidReleaseManifest`.
- **Проблема:** нет явного ограниченного timeout для запроса; UI может долго оставаться в «Проверяем…» при плохой сети.
- **Готово, когда:** при недоступном сервере за ограниченное время появляется понятная ошибка и повтор; работа с маршрутом остаётся доступной.

### PICK-14 · P2 · Профиль водителя зависит от выбранного рейса

- [x] Брать личность и контакты из профиля/справочника водителей.
- **Выполнено 17.09.2026 (этап 9):** API возвращает только актуальную карточку авторизованного водителя: имя, телефоны, перевозчик, без полного справочника. Выбор не зависит от города, дня и наличия рейса. Автомобиль обозначен как снимок рейса на дату. Тесты PostgreSQL и React проверяют отсутствие рейса, другой день/город и изоляцию от чужой карточки.
- **Статус:** по коду / UX-несоответствие.
- **Где:** `src/features/pickup/PickupDriverNavigation.tsx` → `DriverProfile`.
- **Проблема:** используется `route.snapshot.driver`; без маршрута отображение обедняется до логина, а исторический рейс может содержать устаревшие контакты.
- **Готово, когда:** профиль стабилен при смене дня и отсутствии рейсов; сведения конкретного рейса отдельно обозначены как снимок на дату рейса.

### PICK-15 · P2 · На главной водителя не видно возраста данных

- [x] Показывать время последней успешной синхронизации и отдельное состояние ошибки.
- **Выполнено 17.09.2026 (этап 9):** Snapshot хранит время успешной синхронизации, включая офлайн-копию. Главная и маршрут показывают время, ошибку и число ожидающих отметок. Ошибка доступа не маскируется сообщением об отсутствии рейсов. Старый кэш без времени честно обозначен как неизвестный. React-тест проверяет эти состояния.
- **Статус:** по коду.
- **Где:** `src/features/pickup/PickupDriverNavigation.tsx` → `DriverHome`.
- **Проблема:** «Данные последней синхронизации»/«Показаны сохранённые данные» не говорят, насколько данные старые. В GPS-мониторе время уже есть; замечание относится к главной и данным маршрута.
- **Готово, когда:** видны время, состояние сети/сервера и количество ожидающих отметок; ошибка авторизации не выглядит как обычный офлайн.

## 4. Остальные продуктовые и UI-сценарии

### UX-01 · P1 · Авиа-перевозки всегда распознаются как неавиа

- [ ] Восстановить классификацию вида перевозки из подтверждённого поля.
- **Статус:** по коду.
- **Где:** `src/lib/cargoUtils.ts:25` → `isAir`; потребители `cargoTransportType.ts`, `sendingsTransportMode.ts` и расчёт плановых дней.
- **Проблема:** `isAir` всегда возвращает false. Это влияет на классификацию/фильтрацию и выбор нормативного срока там, где используется эта функция.
- **Готово, когда:** реальные обезличенные примеры авто/авиа дают правильные типы и сроки; неизвестный тип не маскируется подтверждённым авто.

### UX-02 · P1 · Скачивание Wildberries обходит выбор API-origin в native

- [ ] Использовать общий механизм скачивания для web и Capacitor.
- **Статус:** код исправлен, пункт остаётся открытым до проверки физических Android/iOS.
- **Этап 10, 17.09.2026:** АПП Wildberries переведён на fetchDownloadDocumentDetailed и saveBlobFile: общий выбор API-origin, авторизация и native Filesystem/Share. В отладку не сохраняется полный base64. Два теста проверяют web/native пути с подменёнными API. Фактическое скачивание в WebView Android/iOS ещё не проверено.
- **Где:** `src/pages/WildberriesPage.tsx:1504`.
- **Проблема:** относительный download URL дополняется `window.location.origin` и запрашивается напрямую. В native это может быть локальный origin приложения вместо backend.
- **Готово, когда:** скачивание одного файла проходит в web, Android и iOS; используется настроенный API-origin и принятый механизм авторизации/сохранения файла.

### UX-03 · P2 · Загрузка сессий возвратов не защищена от устаревшего ответа

- [x] Защитить состояние от завершения предыдущей асинхронной загрузки.
- **Выполнено 17.09.2026 (этап 9):** Загрузки списка/сессии имеют номера запросов; результаты проверяются после каждого await. Смена аккаунта, размонтирование и новая загрузка инвалидируют прежние ответы. Файлы, workbook и ID применяются вместе после гидратации; новые upload-сессии также защищены. Три React-теста воспроизводят A после B, смену аккаунта во время гидратации и начало новой загрузки.
- **Статус:** риск по коду; быстрые переключения и смена аккаунта требуют воспроизведения в UI.
- **Где:** `src/features/haulzReturns/hooks/useHaulzSession.ts:85` → `loadJob`.
- **Проблема:** несколько await и обновлений состояния без request-id/отмены; ответ предыдущей загрузки способен перезаписать workbook после смены актуального контекста.
- **Готово, когда:** искусственно задержанный ответ A после выбора B или смены аккаунта игнорируется; jobId, файлы и workbook всегда принадлежат одной сессии.

### UX-04 · P2 · Модальные формы неполно управляют фокусом и закрытием

- [x] Унифицировать доступное поведение модальных окон и защиту несохранённого ввода.
- **Статус:** закрыт локально; полный аудит скринридеров и устройств не проводился.
- **Этап 10, 17.09.2026:** Общий GuardedDialog для pickup-редакторов и новой заявки: native modal, доступное имя, Tab/Shift+Tab, Escape, возврат фокуса, блокировка закрытия при сохранении и подтверждение потери ввода. Контракты покрыты React-тестами. Во встроенном браузере проверены открытие обеих форм, модальная изоляция и возврат фокуса; системная клавиатура/Back на телефоне остаются ручной проверкой.
- **Где:** редакторы `src/features/pickup/PickupPage.tsx`, `src/features/documents/orders/NewOrderModal.tsx`.
- **Проблема:** нет полного набора focus trap/возврата фокуса/Escape/доступного названия; backdrop-close в форме заявки может закрыть несохранённую форму.
- **Готово, когда:** диалог имеет имя, фокус остаётся внутри, после закрытия возвращается к вызывающей кнопке; случайное закрытие заполненной формы не теряет работу.

### UX-05 · P2 · Плотные кнопки финансовых таблиц неудобны для касания

- [x] Увеличить зоны нажатия и унифицировать цветовые роли действий.
- **Статус:** закрыт локально.
- **Этап 10, 17.09.2026:** Кнопки действий увеличены до 44 px, второстепенные операции собраны в «Ещё», удаление отделено и сохраняет подтверждение. Цвета берутся из общих theme-токенов; фокус выделен. Во встроенном браузере измерена высота 44 px и проверены светлая/тёмная темы. Контраст текста: обычный 16,83/15,63:1, опасный 5,24/6,03:1, белый на синем 5,17:1.
- **Где:** `src/features/admin/components/AdminExpenseRequestsTable.tsx:158–204`.
- **Проблема:** мелкий шрифт около 0.68rem, небольшие padding и много соседних действий; цвета заданы локально вместо общей системы.
- **Сделать:** касаемая область порядка 44–48px, второстепенные действия в меню, опасные действия отдельно; общие токены статусов, подпись/иконка в дополнение к цвету.
- **Готово, когда:** действия удобны на узком экране; проверены светлая/тёмная темы, фокус и контраст. Не считать текущий контраст нарушением без измерения.

### UX-06 · P2 · Экран ошибки раскрывает технические детали по умолчанию

- [x] Сделать восстановление после ошибки понятным обычному пользователю.
- **Выполнено 17.09.2026 (этап 9):** Первым показано понятное объяснение, повтор и ID ошибки (такой же в console.error); технические сведения свёрнуты. Сброс входа явно объясняет последствия и требует подтверждения. React-тест проверяет свёрнутые детали и отсутствие очистки при отказе.
- **Статус:** по коду.
- **Где:** `src/components/ErrorBoundary.tsx:73`.
- **Проблема:** раскрытый `<details open>` показывает stack/component stack и перегружает аварийный экран.
- **Готово, когда:** сначала видны простое объяснение, повтор/возврат и ID ошибки; технические подробности свёрнуты или доступны поддержке; последствия очистки данных объясняются до действия.

## 5. Технический долг, сборка и проверяемость

### TECH-01 · P1 · TypeScript не проходит, а frontend проверяется неполно

**Этап 11, частично:** команда typecheck:frontend проверяет весь src; остаётся 300 диагностик (было 369). Проверка backend отдельно проходит; строгая frontend-проверка пока не добавлена как обязательный зелёный CI-шаг.

- [ ] Исправить ошибки и сделать типизацию обязательным отдельным этапом.
- **Прогресс 17.09.2026: частично выполнено.** Все 13 перечисленных ниже диагностик исправлены, `tsc --noEmit` проходит. Остаётся расширить конфигурацию на весь frontend и сделать проверку обязательной перед merge.
- **Статус:** воспроизведено.
- **Где:** `tsconfig.json`, `package.json` и файлы из списка ниже.
- **Проблема:** успешная Vite-сборка не означает успешную типизацию; текущий общий tsconfig не охватывает весь frontend. Помимо импортов API есть ошибки типов и потенциально undefined-значения.
- **Готово, когда:** backend и весь frontend проходят свои согласованные typecheck-конфигурации; обязательная проверка выполняется перед merge. Не скрывать диагностики массовыми `any`/ignore.

Диагностики общего `tsc --noEmit`:

1. `api/admin-me.ts:18` — payload может быть null.
2. `api/admin-push-templates.ts:81` — unknown-поля не соответствуют контракту шаблона.
3. `api/partner/v1/orders.ts:7` — отсутствующий модуль.
4–9. `api/partner/v1/orders/create.ts:2–10` — шесть неверных импортов.
10. `lib/documentsOrderSubmitPending1c.ts:2` — импорт неэкспортируемого `HaulzCalcDraftRow`.
11. `lib/haulzCalculator/boxPresets.ts:55` — string вместо допустимого union размеров.
12. `lib/haulzCalculator/publicEstimate.ts:90` — `mainlineOptions` может быть undefined.
13. `src/api/client/admin/lastMileReport.ts:1` — отсутствует расширение импорта для NodeNext.

Проблемы импортов партнёрского API также выделены в API-01 из-за функционального последствия; это не отдельный второй дефект.

### TECH-02 · P2 · Тест форматирования суммы зависит от вида пробела

- [x] Зафиксировать контракт форматирования и стабилизировать тест уведомлений.
- **Выполнено 17.09.2026:** результат `Intl.NumberFormat` нормализуется до обычного пробела в обоих путях формирования сводки; тест проверяет полный текст и проходит.
- **Статус:** воспроизведено.
- **Где:** `lib/notificationDailySummary.test.ts:120`.
- **Проблема:** ожидание `12 345 ₽` расходится с `12 345 ₽` из-за неразрывного пробела. Неверная сумма этим тестом не доказана.
- **Готово, когда:** выбран и явно проверен нужный формат; тест проходит в целевых окружениях без ослабления проверки содержания уведомления. Второй падающий тест разобран в API-09.

### TECH-03 · P2 · Недостаточно автоматических проверок пользовательских сценариев

- [ ] Добавить проверки ролей, восстановления и критичных UI-переходов.
- **Статус:** частично выполнен; нужен успешный E2E/CI и обязательная проверка ветки.
- **Этап 10, 17.09.2026:** Добавлены команда test:e2e:guest, автоматический запуск Vite и workflow Checks (unit/API, серверный tsc, build, mobile guest smoke). Тесты больше не пропускаются молча при недоступном сервере. Расширены React-контракты очереди, подтверждения 409, диалогов и native скачивания. Локальный Chromium установлен, но запуск блокирует macOS sandbox (MachPort Permission denied). GitHub workflow ещё не запускался; обязательность check в защите main нужно включить отдельно. Пункт не закрыт.
- **Где:** `package.json`, `vitest.config.*`, `playwright.config.*`, `e2e/`.
- **Проблема:** Vitest в основном проверяет `.test.ts` в node; E2E ориентирован на гостевой мобильный сценарий. Команда `test:e2e:guest`, упомянутая документацией, отсутствует в scripts. Репозиторной `.github`-конфигурации не найдено; это не доказывает отсутствие внешнего CI.
- **Готово, когда:** документированная команда запускается; обязательные проверки покрывают матрицу ролей/чужих данных, форму водителя, 409/офлайн, поворот, завершение с заявкой и ошибки внешнего API. Есть согласованный обязательный CI-проход.

### TECH-04 · P2 · Крупный JS-чанк ухудшает потенциал мобильной загрузки

**Этап 11, частично:** стартовый JS около 204 КБ gzip; бюджет 225000 байт gzip/750000 raw, проверка статического графа в CI. Реальные мобильные метрики отсутствуют.

- [ ] Ввести бюджет сборки и проверить загрузку на слабом устройстве.
- **Статус:** размер воспроизведён, реальная задержка не измерялась.
- **Где:** результат `npm run build`, импорты страниц и тяжёлых библиотек.
- **Данные:** крупнейший `index` — около **1015.64 kB**, gzip **327.01 kB**; другой index около 415.29 kB, gzip 107.96 kB. Vite предупреждает о размере чанков.
- **Сделать:** анализ состава, отложенная загрузка тяжёлых функций по действию, бюджет начальной загрузки; не дробить автоматически без измерения.
- **Готово, когда:** зафиксированы метрики на мобильной сети/устройстве и пороги регрессии; старт основного сценария не требует необязательных модулей.

### TECH-05 · P2 · Крупные компоненты смешивают UI, операции и состояние

- [x] Поэтапно выделить границы модулей с сохранением поведения.
- **Статус:** закрыт текущий этап выделения модулей; дальнейший рефакторинг возможен.
- **Этап 10, 17.09.2026:** Из PickupPage выделены sendOutbox, usePickupOutbox и PickupOutbox — отправка, хранение по аккаунтам и UI сверки конфликтов. Хук скрывает очередь другого аккаунта сразу при переключении и не применяет запоздалое сохранение к новому пользователю (отдельный React-тест). Выделены также timedAssessment, mutationLocks и GuardedDialog. Сценарии очереди теперь меняются и проверяются без редактирования монолитной страницы.
- **Где:** `src/pages/WildberriesPage.tsx` (~2863 строки), `src/features/pickup/PickupPage.tsx` (~2622), `api/pickup/index.ts` (~1597), `src/features/pickup/Forms.tsx` (~1429).
- **Проблема:** сложнее проверять конкурирующие изменения, разрешения и состояния ошибок; ветвление мобильного/desktop поведения уже приводит к PICK-02.
- **Сделать:** выделять команды, загрузку/синхронизацию, устойчивые черновики и представления по пользовательским сценариям. Рефакторить после фиксации контрактов тестами, не переписывать приложение целиком.
- **Готово, когда:** существенный сценарий можно изменить и проверить без редактирования монолитной страницы/API; правила прав и статусов имеют один источник.

### TECH-06 · P2 · Web-версия не идентифицирует конкретный выпуск

- [x] Добавить build ID/commit и время сборки в сведения о версии.
- **Выполнено 17.09.2026 (этап 9):** Каждая Vite-сборка получает уникальный ID, commit (если доступен) и UTC-время. build-info.json и встроенный ID создаются одним источником; сведения доступны в общем и водительском профиле. Web-проверка различает текущую/новую сборку и недоступность сервера. В изолированной сборке без .git commit=unknown, уникальный ID и время сохраняются.
- **Статус:** по коду / эксплуатационный долг.
- **Где:** `package.json`, `vite.config.*`, отображение версии в профиле.
- **Проблема:** web-версия берётся из package version `1.0.0`; разные сборки могут показывать одинаковое значение. Android versionCode решает другую задачу.
- **Готово, когда:** поддержка по профилю определяет конкретный web/native выпуск; проверка обновления различает актуальный и устаревший build.

---

## Порядок работ

1. **Граница доступа:** SEC-01–05; отрицательные тесты до исправлений, затем единый серверный протокол сессий (SEC-06). Отдельно проверить доступ каждого чувствительного endpoint по ролям.
2. **Сохранность работы и данных:** SEC-07, API-01–05, API-09, PICK-01–05, PICK-11, UX-01–02.
3. **Надёжность выпуска:** TECH-01–03; проверки на настоящих устройствах и интеграционном стенде.
4. **Удобство и эксплуатация:** оставшиеся P2, начиная со свежести данных, окон маршрута, обновлений и разрешения конфликтов.

## Обязательная ручная проверка после исправлений

Это план верификации, а не список уже доказанных дефектов.

- [ ] Роли: обычный клиент, диспетчер, водитель, администратор; вход по прямой ссылке и прямые API-запросы. Снятие бейджа должно отзывать доступ.
- [ ] Водитель: текущий день, отсутствие рейсов, несколько рейсов, 20 точек, изменение порядка, частичный забор, фото, конфликт изменений диспетчера.
- [ ] Сдача на склад: с заполненной заявкой завершается; без заявки требуется ввод; сервер не позволяет обойти условие.
- [ ] Офлайн: сеть пропала до фото/после фото/после отправки, повтор после timeout, конфликт 409, заполненное хранилище, перезапуск приложения.
- [ ] GPS: первый запуск, отказ разрешения, приблизительная точность, скачок, навигатор поверх приложения, блокировка экрана; устаревшая/подозрительная позиция не выдаётся за текущую точную.
- [ ] Маршруты: произвольный старт, конечный склад, окна, ожидания, ночная смена, ограничения ТС, недоступность/лимит 2ГИС, ручное принятие предложенного порядка.
- [ ] Web/Android/iOS: обновление, скачивание документов, камера, возврат из фонового режима, клавиатура, safe area, поворот и планшет.
- [ ] UI: 320/390/768px и desktop, светлая/тёмная темы, длинные названия/адреса, масштаб текста, клавиатура, screen reader, измерение контраста. Статусы различимы не только цветом.
- [ ] Интеграции: sandbox 1С/2ГИС/уведомлений; отказ, timeout, повтор; отсутствие дублирующих операций и понятная индикация результата.
- [ ] Финансовые операции и документы: отдельная проверка прав, переходов статусов и сверка итоговых сумм на обезличенных бизнес-примерах — выборочного ревью UI недостаточно для утверждения об их полной корректности.
