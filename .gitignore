.npm-cache/
node_modules
.vercel
# Сборка Vite — не в git (Layero/Vercel собирают сами)
dist/
# Android signing (local only)
android/keystore.properties
android/*.jks
android/*.keystore
android/app/google-services.json
ios/App/App/GoogleService-Info.plist
# macOS
.DS_Store
**/.DS_Store
# Секреты и локальные env — не коммитить (файл .env у разработчика остаётся только локально).
.env*.local
.env
.env.vercel
.env.timeweb
.env*
test-results/
playwright-report/
