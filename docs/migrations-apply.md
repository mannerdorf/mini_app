# Применение миграций

Если функция «Паром» в разделе Документы → Отправки не работает и появляется ошибка  
«Таблица sendings_ferry не найдена», нужно применить миграции к базе данных.

## Оглавление

- [Миграции для паромов](#миграции-для-паромов)
- [Как применить](#как-применить)
- [Проверка](#проверка)
- [Важно про run_all.sql](#важно-про-run_allsql)
- [Устранение неполадок](#устранение-неполадок)

## Миграции для паромов

1. **049_ferries.sql** — создаёт таблицу `ferries` (справочник паромов)
2. **050_sendings_ferry.sql** — создаёт таблицу `sendings_ferry` (привязка парома к отправке)

Порядок важен: 050 зависит от 049 (foreign key на `ferries.id`).

### Связанная миграция

**039_add_ferry_auto_categories.sql** — добавляет статьи расходов «Паром» и «Авто» для PnL. Если у вас есть таблицы `expense_categories` и `pnl_expense_categories`, но в PnL нет статей «Паром»/«Авто», выполните эту миграцию отдельно (она не зависит от 049/050).

## Как применить

### Вариант 1: Vercel Postgres

1. Откройте [Vercel Dashboard](https://vercel.com) → проект → Storage → Postgres
2. Выберите вкладку «Query» (или «Query Editor»)
3. Выполните содержимое файлов в порядке:
   - `migrations/049_ferries.sql`
   - `migrations/050_sendings_ferry.sql`

### Вариант 2: psql

```bash
# Подключение (подставьте свои значения из Vercel или .env)
psql "postgres://user:password@host:port/database?sslmode=require"

# Выполнить миграции
\i migrations/049_ferries.sql
\i migrations/050_sendings_ferry.sql
```

### Вариант 3: run_all.sql

Если база пустая или нужны все миграции:

```bash
psql "postgresql://..." -f migrations/run_all.sql
```

## Проверка

После применения таблицы должны существовать:

```sql
SELECT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_name = 'ferries');
SELECT EXISTS (SELECT 1 FROM information_schema.tables WHERE table_name = 'sendings_ferry');
```

Обе должны вернуть `t` (true).

## Важно про run_all.sql

Файл `run_all.sql` выполняет **все** миграции подряд. Используйте его только если:
- база полностью пустая (новый проект), или
- вы уверены, что нужны все миграции.

Если таблицы уже существуют, `run_all.sql` может вызвать ошибки дублирования. В этом случае применяйте только нужные миграции (049 и 050) вручную.

## Устранение неполадок

**Ошибка «relation already exists»**  
Таблицы уже созданы — миграции применены. Ошибка «sendings_ferry не найдена» может означать, что приложение подключается к другой базе (другой `DATABASE_URL`) или используется другой schema. Проверьте переменные окружения.

**Ошибка «permission denied»**  
Убедитесь, что пользователь БД имеет права на создание таблиц в выбранной схеме (обычно `public`).

**После миграций функция всё ещё не работает**  
Перезапустите приложение (Vercel deployment или локальный сервер), чтобы подхватить изменения в БД.
